<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd" >
<suite name="ParallelSuite" parallel="tests" thread-count="2">

	<!-- Chrome test: runs all classes found under package m1 using Chrome -->
	<test name="Chrome Test">
		<parameter name="browser" value="chrome" />
		<packages>
			<!-- scans all test classes under package 'm1' -->
			<package name="m1" />
		</packages>
	</test>

	<!-- Firefox test: runs all classes found under package m1 using Firefox -->
	<test name="Firefox Test">
		<parameter name="browser" value="firefox" />
		<packages>
			<!-- scans all test classes under package 'm1' -->
			<package name="m1" />
		</packages>
	</test>

	<!-- listeners at the suite level -->
	<listeners>
		<listener class-name="listeners.ExtentListener" />
	</listeners>

</suite>